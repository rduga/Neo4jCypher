package neo4j.cypher.behavior;

/*Generated by MPS */

import jetbrains.mps.smodel.SNode;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SPropertyOperations;

public class INamedPropertyContainer_Behavior {
  public static void init(SNode thisNode) {
  }

  public static boolean call_isCorrectIdentifierName_5206628397327339026(SNode thisNode) {
    boolean isEscapeMode = false;
    boolean isUnicodeMode = false;
    boolean isSymbolCodeMode = false;
    int digitNumber = 0;
    int unicodeDigitNumber = 0;
    String value = SPropertyOperations.getString(thisNode, "name");

    if (value == null) {
      return true;
    }

    // backtited value 
    if (value.length() >= 2 && value.charAt(0) == '`' && value.charAt(value.length() - 1) == '`') {
      // check inner pairing of backtites 
      String inner = value.substring(1, value.length() - 1).replace("``", "");
      if (inner.contains("`")) {
        return false;
      }

      return true;
    }

    // no backtited value - only alphanumeric chars, first char must be alpha 
    if (value.length() > 0) {
      if (!(value.matches("[a-zA-Z][a-zA-Z0-9]*"))) {
        return false;
      }
    }

    return true;
  }
}
